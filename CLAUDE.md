# Auto-Planning 프로젝트 컨텍스트

## 최종 업데이트: 2026-01-16

---

## 프로젝트 개요
외주처 생산계획 자동화 시스템 (Excel VBA 매크로)

**핵심 문서: [FRAMEWORK.md](./FRAMEWORK.md)** - v8.1 설계 프레임워크

---

## v8.1 구현 완료 (2026-01-16)

### 주요 변경사항 (v8.0 → v8.1)
- **이동일 = 사용자 입력** (기존: 납기에서 역산)
- **순산 로직** (이동일 → 생산완료 → 납기 체크)
- **납기준수 컬럼 추가** (OK / 위험 / 불가)
- **1코드 납기 자동 적용** (세트품에서 9코드 납기 따라감)

### 파일
- **`excel-vba/AutoPlanning_v8.1.bas`** - 최신 버전 (약 2400줄)

### 제공 매크로 (사용자 실행용)
| 매크로명 | 설명 |
|---------|------|
| `초기설정` | 시트 구조 생성 (Raw데이터, 설정_외주처, 설정_고객매칭) |
| `자동배정실행` | Raw데이터 기반 자동배정 → 생산계획표 생성 |
| `업체별계획표생성` | 업체별 별도 시트 생성 (리니어_계획표, 위드맘_계획표 등) |
| `전체리포트생성` | 자동배정 + 업체별계획표 한번에 실행 |

### 구현된 기능
| 기능 | 상태 | 설명 |
|------|------|------|
| Raw 데이터 시트 | ✅ | 18개 컬럼, 드롭다운, 조건부서식 |
| 설정 시트 | ✅ | 외주처 정보, 고객매칭 |
| **순산 로직** | ✅ | 이동일 → 생산시작 → 생산완료 → 납기 체크 |
| 세트품 처리 | ✅ | 연속행 + 동일 고객사코드, 80% capa |
| **1코드 납기 자동** | ✅ | 빈칸이면 9코드 납기 따라감 |
| 자동배정 | ✅ | 고객매칭 우선, 우선순위 순, 슬롯 검색 |
| D+2 확정구간 | ✅ | 확정/조정가능 구간 구분 |
| 업체 유지 | ✅ | 이동일 변경 시 기존 업체 우선 |
| 경고 시스템 | ✅ | 특수공정 불가, 고객매칭 불일치 |
| **납기준수 체크** | ✅ | OK / 위험 / 불가 표시 |
| 배정불가 대안 | ✅ | 대안 업체 리스트 제공 |
| 생산계획표 | ✅ | 달력 뷰, 세련된 디자인 |
| 업체별 계획표 | ✅ | 업체별 별도 시트, 변경사항 하이라이트 |

### 핵심 제약조건 (비즈니스 룰)
- **이동일 = 사용자 결정** (자재/벌크 상황에 따라)
- **납기 = 절대 고정** (고객 지정, 무조건 맞춰야 함)
- **발주완료 = 절대 고정** (자재 이동 후 변경 어려움)
- **D+2 이내 = 확정** (변경 시 개별 연락)
- **D+3 이후 = 조정 가능** (일괄 통보)

---

## 시트 구조

### Raw데이터 시트 (입력) - v8.1

**사용자 입력 (A~K)**
| 컬럼 | 필드 | 설명 |
|------|------|------|
| A | 품번 | 숫자+영어3자리+숫자 |
| B | 품명 | 제품명 |
| C | 수량 | 생산 수량 |
| D | **이동일** | **사용자 입력** (자재 이동일) |
| E | 납기 | 고객 납기일 (1코드는 빈칸 가능) |
| F | 공정명 | 포장/충전/충포장 등 |
| G | 특수공정 | 수축/교반/고주파 |
| H | 지정업체 | 빈칸=자동배정 |
| I | 긴급도 | 빈칸=일반, 급함/긴급 |
| J | 상태 | 빈칸=신규, 보류/배정완료/발주완료 |
| K | 비고 | 메모 |

**자동 생성 (L~R)**
| 컬럼 | 필드 | 설명 |
|------|------|------|
| L | 고객사코드 | 품번에서 추출 |
| M | 배정업체 | 자동배정 결과 |
| N | 생산시작일 | 이동일 + 1 |
| O | 생산완료일 | 생산시작 + 소요일 |
| P | **납기준수** | OK / 위험 / 불가 |
| Q | 최초배정업체 | 변경 추적용 |
| R | 업체변경 | 유지/변경됨 |

### 설정_외주처 시트
- 외주처ID, 외주처명, 라인수, 라인당일일Capa, 가능공정, 월생산목표, 우선순위

### 설정_고객매칭 시트
- 고객사코드, 외주처ID, 우선순위

### 생산계획표 시트 (출력)
- 달력 형식의 전체 생산계획 뷰
- 업체별/라인별 배치
- 배정불가 항목 섹션
- **납기 상태별 색상 표시**

### [업체명]_계획표 시트 (출력)
- 업체별 별도 시트
- 이동일 기준 리스트 형식
- 변경사항 하이라이트 (신규 추가, 변경)
- **납기준수 컬럼 표시**

---

## 순산 로직 (v8.1)

```
이동일(입력) → 생산시작(+1) → 생산완료 → 납기 체크

납기준수 판정:
├─ 생산완료 ≤ 납기-1일 → OK (정상)
├─ 생산완료 = 납기     → 위험 (당일직납)
└─ 생산완료 > 납기     → 불가 (납기 초과)
```

---

## 세트품 처리

```
세트품 구성:
┌─ 9코드 (포장/충포장) → 납기 있음 ← 기준
└─ 1코드 (충전/충포장) → 납기 빈칸 → 9코드 납기 자동 적용

규칙:
- 연속된 행 + 동일 고객사코드
- 1코드 납기/이동일 빈칸 → 9코드 값 사용
- 합산 수량으로 Capa 계산 (80%)
```

---

## 경고 시스템

| 경고 타입 | 조건 | 표시 |
|----------|------|------|
| `PROCESS_UNABLE` | 수동 지정 업체가 특수공정 처리 불가 | 진한 빨간 배경 |
| `CLIENT_MISMATCH` | 고객매칭 설정과 다른 업체 수동 지정 | 연한 빨간 배경 |

---

## 배정 로직 흐름

```
H열에 지정업체 있음?
    ├─ YES → 해당 업체에서만 슬롯 검색
    │         └─ 슬롯 없음 → "배정불가" (fallback 안함)
    │         └─ 경고 체크 (특수공정/고객매칭)
    │
    └─ NO → 자동 배정
              ├─ 1순위: 기존 배정 업체 (이동일 변경 시)
              ├─ 2순위: 고객매칭 업체
              └─ 3순위: 우선순위 순 검색
```

---

## 파일 구조

```
auto-planning/
├── CLAUDE.md              # 이 파일 (프로젝트 컨텍스트)
├── FRAMEWORK.md           # v8.1 설계 프레임워크 (상세)
├── excel-vba/
│   ├── AutoPlanning_v8.1.bas  # ★ 최신 버전
│   ├── AutoPlanning_v8.bas    # 이전 버전
│   ├── AutoPlanning_v7.bas
│   └── ...
└── src/                   # Next.js 웹앱 (참고용)
```

---

## 사용 방법

### 최초 설정
1. Excel 파일 생성
2. VBA 편집기 열기 (Alt+F11)
3. `AutoPlanning_v8.1.bas` 파일 import
4. `초기설정` 매크로 실행

### 일일 사용
1. `Raw데이터` 시트에 신규 건 입력 (**이동일, 납기 포함**)
2. `자동배정실행` 또는 `전체리포트생성` 매크로 실행
3. `생산계획표` 시트에서 전체 현황 확인
4. **납기준수 컬럼 확인** (불가/위험 건 주의)
5. `[업체명]_계획표` 시트를 업체에게 공유

---

## TODO

- [x] Raw 데이터 시트 템플릿 생성
- [x] 역산 로직 VBA 구현 → **순산 로직으로 변경 (v8.1)**
- [x] 세트품 식별 로직 VBA 구현
- [x] **1코드 납기 자동 적용 (v8.1)**
- [x] 자동배정 로직 VBA 구현
- [x] **납기준수 체크 기능 (v8.1)**
- [x] 배정불가 대안 리스트 출력 기능
- [x] 업체별 계획표 자동 생성 기능
- [ ] **실제 데이터로 테스트 후 피드백 반영** ← 다음 단계

---

## 메모

- **v8.1 핵심 변경**: 이동일이 사용자 입력으로 변경됨 (현실 반영)
- 1코드는 보통 납기가 없음 → 같은 세트의 9코드 납기 자동 적용
- 납기는 절대 고정 (늦게 이동해도 납기는 무조건 맞춰야 함)
- 업체별 계획표는 별도 시트 방식으로 구현 (업체에게 개별 공유 용이)
